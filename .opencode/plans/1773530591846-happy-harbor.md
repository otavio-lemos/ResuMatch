# Plano: Timeout Inteligente para Ollama

## Problema
Quando Ollama não está rodando, o sistema espera 10 minutos antes de retornar erro.

## Solução
Dois timeouts:
1. **Conexão**: 15 segundos - detecta rápido se Ollama existe
2. **Processamento**: 10 minutos - só após conexão bem-sucedida

## Implementação

### parse-resume/route.ts
```javascript
// Timeout curto para conexão (15s)
const connectionController = new AbortController();
const connectionTimeout = setTimeout(() => connectionController.abort(), 15000);

try {
  const response = await fetch(ollamaUrl, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    signal: connectionController.signal,
    body: JSON.stringify({...})
  });
  
  clearTimeout(connectionTimeout);  // Conectou!
  
  // Processa stream (até 10 min)
  // ... código existente
  
} catch (err) {
  if (err.name === 'AbortError') {
    throw new Error('Ollama não está conectado. Verifique se o Ollama está rodando (execute "ollama serve" no terminal).');
  }
  throw err;
}
```

### analyze/route.ts
Mesma lógica.

## Resultado
- Se Ollama não existe: erro em ~15s
- Se Ollama existe mas lento: até 10min de processamento
